const express = require('express');
const router = express.Router();

router.post('/register', (req, res, next) => {
  // findUser
  // if User exist, return response with email exist try logging in
  // else
  // encrypt the password
  // save the hash
  //
});

module.exports = router;
